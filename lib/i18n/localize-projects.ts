import type { Project } from '@/lib/types/project'

export function localizeProject(project: Project, _locale?: string): Project {
  return project
}

export function localizeProjects(projects: Project[], _locale?: string): Project[] {
  return projects
}
